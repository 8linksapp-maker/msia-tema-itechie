---
export const prerender = false;
import AdminNav from '../components/admin/AdminNav';
import CmsToaster from '../components/admin/CmsToaster';
import '../styles/admin.css';

interface Props {
    title?: string;
    activeSection?: string;
}
const { title = 'Admin', activeSection = '' } = Astro.props;
---
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} — Admin</title>
    <link rel="stylesheet" href="/itechie-assets/css/custom-icon.css" />
    <link rel="stylesheet" href="/itechie-assets/css/fontawesome.min.css" />
    <style>
        /* Quill override */
        .ql-toolbar { border-radius: 8px 8px 0 0 !important; border-color: #e2e8f0 !important; }
        .ql-container { border-radius: 0 0 8px 8px !important; border-color: #e2e8f0 !important; min-height: 300px; }
        .ql-editor { font-size: 15px; line-height: 1.7; }
        
        /* IconPicker preview fix */
        [class^="icomoon-"], [class*=" icomoon-"] {
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body class="bg-slate-50 min-h-screen">
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <AdminNav client:load activeSection={activeSection} />
        <!-- Main Content -->
        <main class="flex-1 min-w-0 lg:ml-64 transition-all duration-300">
            <div class="p-4 lg:p-8">
                <slot />
            </div>
        </main>
    </div>
    <CmsToaster client:load />
</body>
</html>
